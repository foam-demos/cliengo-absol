# Chatbot Orchestrator

Core service managing chatbot conversation flows, state management, and routing across channels (web, WhatsApp, Facebook, Instagram). Built with Node.js/TypeScript and Express, with PostgreSQL for conversation persistence. Run locally with `npm install && npm run dev`.