# Messaging Gateway

Handles webhook integrations for WhatsApp Business API, Facebook Messenger, and Instagram Direct. Parses incoming messages, normalizes payloads, and routes to the orchestrator. Built with Express and deployed on Heroku. Run with `npm install && npm start`.