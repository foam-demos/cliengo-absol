# Web Widget API

Serves the embeddable JavaScript chat widget, manages visitor sessions, and handles real-time messaging via WebSockets. Built with Express, Socket.io, Sequelize, and PostgreSQL. Run locally with `npm install && npm run dev`.