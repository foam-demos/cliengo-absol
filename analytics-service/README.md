# Analytics Service

Aggregates conversation metrics, conversion funnels, and campaign performance. Generates reports for dashboards and exports. Built with Node.js, Express, and PostgreSQL with scheduled jobs for aggregation. Run with `npm install && npm start`.