# CRM API

Manages leads, contacts, campaigns, and external CRM integrations (HubSpot, Salesforce). Exposes REST API for CRUD operations on customer data. Built with Express, Sequelize, and PostgreSQL. Run locally with `npm install && npm run dev`.